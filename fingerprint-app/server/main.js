// server/main.js
import { Meteor } from 'meteor/meteor';
import '/imports/api/api'; // Ensure the path matches your directory structure

Meteor.startup(() => {
  // code to run on server at startup
});
